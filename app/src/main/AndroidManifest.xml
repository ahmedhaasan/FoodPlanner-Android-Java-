<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/fifth192"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fifth192"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodPlanneritiAndroidJava"
        tools:targetApi="31">
        <activity
            android:name=".view.splashScreen.SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- MainActivity as the entry point -->
        <activity
            android:name=".view.login_signUp.MainActivity"
            android:exported="true">

        </activity> <!-- HomeActivity (without intent filters) -->
        <activity
            android:name=".view.homeActivity.HomeActivity"
            android:exported="true"></activity>
    </application>

</manifest>